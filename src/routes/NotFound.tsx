import MainLayout from '@/layouts/MainLayout';
import Hero from '@/components/home/HeaderSection';

const NotFound: React.FC = () => {
  return (
    <MainLayout>
      <Hero />
    </MainLayout>
  );
};

export default NotFound;
